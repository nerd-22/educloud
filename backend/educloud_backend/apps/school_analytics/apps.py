from django.apps import AppConfig

class SchoolAnalyticsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.school_analytics'  # Must match the dotted path to your app
